#include <stdlib.h>
#include <time.h>
#include<stdio.h>

/**
 *main-Start the program
 *Return 0 if success
 */
int main (void)
{
int n;
int lastnum;
srand(time(0));
n = rand() - RAND_MAX / 2;
lastnum = n % 10;	
return (0);
}
