#include "main.h"
/*
 *_atoi - Convert a string to an integer
 *@s: String to convert
 *Return: Integer after conversion
 */
int _atoi(char *s)
{
  int i, j, len;
  i = 0;
  while (s[i] != '\0')
    { 
      i++;
    }
  len = i;
 
}
