#include"main.h"
/**
 *_islower- Checks for lowercase characters
 *@int: returns 1 if lowercase, 0 if uppercase
 */
int _islower(int)
{
char r;
if ('r' <= 97 && 'r' <= 122)
{
return (1);
}
else
{
return (0);
}
}
